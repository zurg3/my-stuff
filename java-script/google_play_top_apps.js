var top_len = prompt('Enter list length');
var top_num = 1;
for (var i = 0; i < top_len; i++) {
  console.log(top_num + ". " + document.getElementsByClassName('WsMG1c nnK0zc')[i].innerText);
  var top_num = top_num + 1;
}
